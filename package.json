{
  "name": "cliver",
  "private": true,
  "type": "module",
  "workspaces": ["prototypes/*"],
  "dependencies": {
    "@cliver/contracts": "*",
    "@cliver/p2-pipeline": "*",
    "@cliver/executors": "*",
    "zod": "^3.24",
    "zod-to-json-schema": "^3.24"
  },
  "devDependencies": {
    "typescript": "^5.8",
    "vitest": "^3.0"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  }
}
