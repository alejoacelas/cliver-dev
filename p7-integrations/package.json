{
  "name": "@cliver/p7-integrations",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit --project tsconfig.json",
    "contract-check": "tsc --noEmit --project tsconfig.json"
  },
  "dependencies": {
    "@cliver/contracts": "link:../p0-contracts",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
